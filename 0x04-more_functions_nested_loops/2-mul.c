#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: first interger to check
 * @b: second integer to be checked
 *
 * Return: Always return the result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
